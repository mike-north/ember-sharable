import defaultMetaTags from 'dummy/utils/default-meta-tags';
import { module, test } from 'qunit';

module('Unit | Utility | default meta tags');

// Replace this with your real tests.
test('it works', function(assert) {
  assert.ok(defaultMetaTags);
});
